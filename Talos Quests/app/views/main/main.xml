<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
  <StackLayout>
    <ListView id="MenuItems" items="{{ MenuItems }}" itemTap="MainMenuTap">
	    <ListView.itemTemplate>
	      <StackLayout>
	        <Label class="MenuItems" text="{{ $value }}" />
	      </StackLayout>
	    </ListView.itemTemplate>
  </ListView>
  </StackLayout>
</Page>
