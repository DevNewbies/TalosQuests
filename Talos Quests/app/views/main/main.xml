<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
  <StackLayout>
    <ListView items="{{ MainMenu }}" itemTap="MainMenuTap">
	    <ListView.itemTemplate>
	      <StackLayout>
	        <Label class="MainMenu" text="{{ $value }}" />
	      </StackLayout>
	    </ListView.itemTemplate>
  </ListView>
  </StackLayout>
</Page>